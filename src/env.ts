export function parseEnv() {
  console.log("parseEnv");
  // export function parseEnv({ file, vars = {} } = {}) {
  // let envFromFile = {};
  // if (file) {
  //   const filePath = path.resolve(process.cwd(), file);
  //   const buffer = readFileSync(filePath);
  //   envFromFile = dotenv.parse(buffer);
  // }

  // return {
  //   ...process.env,
  //   ...envFromFile,
  //   ...vars,
  // };
}
